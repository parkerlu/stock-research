# 指标移植说明书：买卖很准 & 主力拉升

> 本文档面向 **TradingView Pine Script (v5/v6) 移植**。两个指标均已在本项目
> (Python/pandas) 完成移植并逐值验证，参考实现见:
> - `backend/app/services/tdx/indicators/maimai_henzhun.py`
> - `backend/app/services/tdx/indicators/zhuli_lasheng.py`
> - `backend/app/services/tdx/indicators/zhuli_lasheng_tiqian.py`
>
> 数据口径: A 股日线、**前复权**价格。文末附验证锚点（真实股票的逐日期望值），
> 移植完成后务必用锚点核对。

---

## 0. 通达信函数语义 → Pine 对照表

移植正确性的关键在于函数语义，先读这张表：

| 通达信 | 语义 | Pine 等价 | 注意 |
|---|---|---|---|
| `MA(X,n)` | 简单均线 | `ta.sma(x, n)` | |
| `EMA(X,n)` | 指数均线, α=2/(n+1) | `ta.ema(x, n)` | |
| `SMA(X,n,m)` | **通达信特有**: `Y = (m·X + (n−m)·Y')/n`，即 α=m/n 的 EMA | 无内置，手写递推（见下） | ⚠️ 与 Pine 的 `ta.sma` **完全不同** |
| `HHV(X,n)` | n 日内最高 | `ta.highest(x, n)` | 见 §0.2 部分窗口 |
| `LLV(X,n)` | n 日内最低 | `ta.lowest(x, n)` | 同上 |
| `REF(X,n)` | n 日前的值 | `x[n]` | |
| `SUM(X,n)` | n 日滚动求和 | `math.sum(x, n)` | |
| `ABS/MAX/MIN` | 逐元素 | `math.abs/max/min` | |
| `IF(C,A,B)` | 三元 | `C ? A : B` | |
| `X CROSS Y` | 上穿 | `ta.crossover(x, y)` | |
| 布尔值参与 HHV/LLV | `HHV(cond,n)` = 近 n 日**曾**成立(窗口 OR)；`LLV(cond,n)` = 近 n 日**全部**成立(窗口 AND) | `math.sum(cond?1:0, n) > 0` / `== n` | 通达信把布尔当 0/1 数值 |
| `X / 0` | **通达信约定结果为 0**（不是 NA/∞） | 手动防护: `d == 0 ? 0 : x / d` | ⚠️ 两个指标都依赖此约定 |

### 0.1 TDX 版 SMA 的 Pine 实现

```pine
// SMA(X, n, m) 通达信递推: Y = (m*X + (n-m)*Y[1]) / n
tdx_sma(float x, int n, int m) =>
    var float y = na
    y := na(y) ? x : (m * x + (n - m) * nz(y[1], x)) / n
    y
```

### 0.2 部分窗口（长周期 HHV/LLV）

通达信惯例：数据不足 n 根时用**现有数据**计算（相当于 pandas `min_periods=1`）。
Pine 的 `ta.highest(x, 500)` 在前 499 根返回 `na`。对历史 ≥500 根的标的，两种算法
从第 500 根起完全一致；若需对齐早期数据，可用：

```pine
// 部分窗口 HHV: 数据不足时取当前可得范围
hhv_partial(float x, int n) =>
    len = math.min(bar_index + 1, n)
    ta.highest(x, len)   // Pine v6 允许动态 length; v5 需 max_bars_back 配合
```

主力拉升用到 `HHV(HIGH,500)`，脚本需声明 `max_bars_back=500`（或
`indicator(..., max_bars_back=500)`）。

### 0.3 颜色换算

通达信 `COLORxxxxxx` 是 **BBGGRR** 顺序（与 RGB 相反）：
`COLOR0066ff` → B=00, G=66, R=ff → RGB `#FF6600`。

---

# 一、买卖很准 (Maimai Henzhun)

价格通道超卖检测 + DMI/ADX 极端过滤。副图，Y 轴固定 0–100。

## 1.1 原始公式（通达信原文）

```
你   := CLOSE;
老   := (LOW + HIGH + CLOSE) / 3;              { 典型价 }
板   := MA(老, 5);
好   := HHV(板, 10);                            { 卖出阈值 = 5日典型均线的10日高点 }
买卖 := LLV(板, 10);                            { 买入阈值 = 5日典型均线的10日低点 }

急卖奇准: IF(HHV(你<好, 5),  100, 50);          { 近5日曾收于卖阈之下 }
短卖奇准: IF(HHV(你<好, 10), 100, 50);          { 近10日曾收于卖阈之下 }
急买奇准: IF(LLV(你<买卖, 5),  50, 0);          { 连续5日收盘全部低于买阈 }
短买奇准: IF(LLV(你<买卖, 10), 50, 0);          { 连续10日全部低于买阈 }

{ DMI / ADX 部分, 周期 5 }
TR  := MAX(MAX(HIGH-LOW, ABS(HIGH-REF(CLOSE,1))), ABS(LOW-REF(CLOSE,1)));
TD  := SUM(TR, 5);
HD  := HIGH - REF(HIGH, 1);
LD  := REF(LOW, 1) - LOW;
DMP := SUM(IF(HD>0 AND HD>LD, HD, 0), 5);
DMM := SUM(IF(LD>0 AND LD>HD, LD, 0), 5);
神偷线    := DMP * 100 / TD;                    { +DI }
辅助线    := DMM * 100 / TD;                    { -DI }
动向趋势线 := MA(ABS(辅助线-神偷线)/(辅助线+神偷线)*100, 3);   { ADX }

准备现金: IF(动向趋势线>88 AND 神偷线<5.8, 80, 0);
```

除零防护：`TD=0` 或 `辅助线+神偷线=0` 时结果取 0（§0 的通达信约定）。

## 1.2 本项目采用的合并版（2026-08-10 优化，**移植目标**）

原版 5 条线信息冗余（急买 LLV5 ⊇ 短买 LLV10；急卖 HHV5 ⊆ 短卖 HHV10，
OR 合并后各由一支决定）。合并为 2 条线：

```
买 = MAX(急买奇准, 短买奇准, 准备现金)     → 取值 0 / 50 / 80
    50 = 连续超卖在场;  80 = 准备现金(ADX极端段)也在场
卖 = MIN(急卖奇准, 短卖奇准)                → 取值 50 / 100 (保留原版取值)
    50 = 急卖<100 OR 短卖<100 → 近期从未跌破卖出阈值 = 强势上涨进行中
```

### 信号规则 — 边沿触发

```
买入信号 = 买[1] > 0   AND 买 == 0      { 连续超卖状态刚结束 → 反转确认 }
卖出信号 = 卖[1] < 100 AND 卖 == 100    { 强势段被打破(重新跌破卖阈) → 止盈离场 }
```

Pine: `buySignal = buyLine[1] > 0 and buyLine == 0`,
`sellSignal = sellLine[1] < 100 and sellLine == 100`。
每段状态只触发一次（可回测、可 `alertcondition`）。
⚠️ 卖出信号在强势段的**结尾**（破位时），不是段首 —— 方向别做反。

## 1.3 绘制规格

| 元素 | 样式 | 颜色 |
|---|---|---|
| 买线 | 阶梯线, linewidth=2 | `#FF3333` |
| 卖线 | 阶梯线, linewidth=2 | `#00CC00` |
| 买入信号 | 上三角 ▲, 画在 y≈8 | `#FF3333` |
| 卖出信号 | 下三角 ▼, 画在 y≈92 | `#00CC00` |
| 水平参考线 | 0 / 50 / 80 / 100 虚线 | 100,0=`#55AA77`; 80=`#FF00FF`; 50=`#888888` |

Pine 建议: `plot(style=plot.style_stepline)`；三角用
`plotshape(location.absolute)` 或固定 `location.bottom / location.top`。

## 1.4 行为特征（用于核对方向是否移植正确）

- 频率量级（603319 实测 9.6 年）: 买 ~7.8 次/年, 卖 ~6.2 次/年。
- 卖线大部分时间停在 100（`close < 5日高点通道` 几乎恒成立），只在强拉升段
  掉到 50；**掉下去不是信号，弹回 100（强势破位）那一刻才是卖出信号**。
- 买 = 连续超卖结束（抄底型）；卖 = 强势段终结（止盈型）。

---

# 二、主力拉升 / 提前预知主力拉升

**两个指标共用同一条计算公式**（已用同花顺实盘读数逐值证实），只差显示方式。
核心输出 `资金入场`：深度超跌 + 创 30 日新低时冒出的反转脉冲，其余时间为 0
（EMA 拖尾衰减）。副图，Y 轴自适应。

## 2.1 原始公式（同花顺"指标代码"导出原文，逐字）

```
Var1:=EMA(HHV(HIGH,500),21);
Var2:=EMA(HHV(HIGH,250),21);
Var3:=EMA(HHV(HIGH,90),21);
Var4:=EMA(LLV(LOW,500),21);
Var5:=EMA(LLV(LOW,250),21);
Var6:=EMA(LLV(LOW,90),21);
Var7:=EMA((Var4*0.96+Var5*0.96+Var6*0.96+Var1*0.558+Var2*0.558+Var3*0.558)/6,21);
Var8:=EMA((Var4*1.25+Var5*1.23+Var6*1.2 +Var1*0.55 +Var2*0.55 +Var3*0.65)/6,21);
Var9:=EMA((Var4*1.3 +Var5*1.3 +Var6*1.3 +Var1*0.68 +Var2*0.68 +Var3*0.68)/6,21);
VarA:=EMA((Var7*3+Var8*2+Var9)/6*1.738,21);
VarB:=REF(LOW,1);
VarC:=SMA(ABS(LOW-VarB),3,1)/SMA(MAX(LOW-VarB,0),3,1)*100;
VarD:=EMA(IF(CLOSE*1.35<=VarA,VarC*10,VarC/10),3);
VarE:=LLV(LOW,30);
VarF:=HHV(VarD,30);
Var10:=IF(MA(CLOSE,58),1,0);
资金入场: EMA(IF(LOW<=VarE,(VarD+VarF*2)/2,0),3)/618*Var10;
资金入场: IF(资金入场>0,资金入场,0),STICK,linethick2,COLOR0000ff;
今量: 资金入场;
a1:IF(资金入场>0,今量*1.2,0),STICK,linethick5,COLOR0000ff;
a2:IF(资金入场>0,今量*0.8,0),STICK,linethick5,COLOR0066ff;
a3:IF(资金入场>0,今量*0.6,0),STICK,linethick5,COLOR0099ff;
a4:IF(资金入场>0,今量*0.4,0),STICK,linethick5,COLOR00ccff;
a5:IF(资金入场>0,今量*0.2,0),STICK,linethick5,COLOR00ffff;
```

## 2.2 每段在算什么（帮助排查移植偏差）

1. **Var1–Var6**: 90/250/500 日高低点通道，各套一层 EMA(21) 平滑。
2. **Var7–Var9 → VarA**: 三组加权（偏低点侧权重更大）再 EMA(21)，最后
   `(3:2:1 加权)/6 × 1.738` 得 **长期估值中枢 VarA**。
3. **VarC**: 反向 RSI —— `SMA(|ΔLOW|,3,1) / SMA(MAX(ΔLOW,0),3,1) × 100`。
   低点连续下挫、毫无反弹时分母→0（按约定结果取 0），反弹出现时值急剧变化。
   衡量下跌的"单边程度"。⚠️ 这里的 SMA 是 **TDX 版**（§0.1），α=1/3。
4. **VarD**: `CLOSE×1.35 ≤ VarA`（股价比中枢低 35%+，深度超跌）时 VarC×10，
   否则 VarC/10；EMA(3) 平滑。
5. **触发**: 仅当 `LOW ≤ LLV(LOW,30)`（当日创 30 日新低）输出
   `(VarD + HHV(VarD,30)×2)/2`，否则 0。
6. **输出**: 上式 EMA(3) 平滑 ÷ 618 × Var10，再钳到 ≥0。
   - `EMA(X,3)` 的 α=0.5 ⇒ **无新触发时每日精确减半**（重要验证特征）。
   - `Var10 := IF(MA(CLOSE,58),1,0)`: MA58 有效（非 NA 非 0）即为 1 —— 取数
     守卫，效果是前 57 根输出 0。Pine: `na(ta.sma(close,58)) ? 0 : 1`。
7. **扇形 a1–a5**: 全部 = `今量 × 固定倍数`，**1.2 / 0.8 / 0.6 / 0.4 / 0.2**。
   纯显示参考档，无独立信息。

## 2.3 两个指标的显示差异

| | 主力拉升 | 提前预知主力拉升 |
|---|---|---|
| 计算 | **同一条公式** | **同一条公式** |
| 资金入场 | 红细线 `#FF0000`, linewidth 2 | 红柱 `#FF0000` (STICK, linethick2) |
| 扇形画法 | 细线, 大写 A1..A5 | **柱状** (STICK, linethick5), 小写 a1..a5 |
| A1/a1 | 绿 `#00B050` | 红 `#FF0000` (COLOR0000ff) |
| A2/a2 | 蓝 `#3C8EFF` | 橙 `#FF6600` (COLOR0066ff) |
| A3/a3 | 黑 `#202020` | 橙黄 `#FF9900` (COLOR0099ff) |
| A4/a4 | 深灰 `#808080` | 金黄 `#FFCC00` (COLOR00ccff) |
| A5/a5 | 浅灰 `#C0C0C0` | 黄 `#FFFF00` (COLOR00ffff) |

Pine 柱状: `plot(style=plot.style_columns)`。**按 a1→a5 从高到低依次绘制**，
高档位先画、低档位覆盖其上，形成红→黄分层渐变柱（TradingView 中后声明的
plot 画在上层，与叠画顺序一致即可）。

⚠️ 主力拉升的独立源码未拿到（只有提前预知的），其配色取自同花顺截图；
数值经实盘读数验证与提前预知完全一致，故共用公式是安全的。

## 2.4 Pine 移植提示

- `max_bars_back=500`（HHV/LLV 500 需要）。
- TDX SMA(…,3,1) 必须用 §0.1 的递推，**不能**用 `ta.sma/ta.rma`
  （`ta.rma(x,3)` 的 α=1/3 恰好相同 —— 可用 `ta.rma` 替代，但注意其初始化
  差异：rma 用 SMA 种子，TDX 用首值种子，前几根会有微小差异后迅速收敛）。
- 除零：`VarC` 分母、`资金入场` 全程按 `x/0=0` 处理（`nz()` + 条件）。
- 结果对复权方式敏感：验证请用**前复权**日线。

---

# 三、验证锚点（移植后必须核对）

标的 **603319（美湖股份, 上证）**，前复权日线。以下为本项目实现的输出，
已与同花顺实盘读数逐值核对一致。

### 3.1 资金入场 / 今量（2026 年 7 月）

| 日期 | 资金入场 | | 日期 | 资金入场 |
|---|---|---|---|---|
| 07-08 | 0.00 | | 07-21 | **70.07** ← 同花顺读数一致 |
| 07-09 | 5.07 | | 07-22 | 35.03 |
| 07-10 | 2.54 | | 07-23 | 17.52 |
| 07-13 | 1.27 | | 07-24 | 8.76 |
| 07-14 | 5.72 | | 07-27 | 4.38 |
| 07-15 | 11.27 | | 07-28 | 35.92 |
| 07-16 | 16.99 | | 07-29 | 52.68 |
| 07-17 | 28.66 | | 07-30 | 63.99 |
| 07-20 | 46.74 | | 07-31 | **31.99** ← 同花顺读数一致 (=63.99×0.5, EMA 减半) |

### 3.2 扇形（2026-07-21, 今量=70.07）

A1=84.08 A2=56.05 A3=42.04 A4=28.03 A5=14.01（= 70.07 × 1.2/0.8/0.6/0.4/0.2）

### 3.3 买卖很准（合并版, 同标的 9.6 年）

- 买线取值 ∈ {0, 50, 80}; 卖线 ∈ {50, 100}
- 买入信号 75 个 (~7.8/年); 卖出信号 60 个 (~6.2/年)
- 最近 5 个买入信号日: 2026-03-10, 04-01, 05-21, 06-30, **07-27**
- 最近 5 个卖出信号日: 2025-12-11, 2026-04-20, 04-28, 05-12, **06-08**
  (06-08 卖出后, 7 月即暴跌 ~35%——方向核对用)

> TradingView 上如无该 A 股标的，可任选波动充分、历史 ≥3 年的标的做行为核对
> （买卖频率比、资金入场"平时为 0、暴跌新低才脉冲、随后逐日减半"的形态），
> 数值锚点则需在有同源数据的环境比对。
