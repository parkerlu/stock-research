# 训练指标

本项目自己训出来的指标。与 `backend/app/services/tdx/indicators/` 下的 TDX 移植指标
区分开：那些是现成公式，这些是从数据里学出来的。

> **定位：动能参考，不是买卖决策依据。**
> 最强的 v5 胜率 62.1%，一年只有约 7.5 次机会。常用的 v4 是 58.3%。
> ⚠️ 2026-09-05 修正: 原先标称的 76.2% / 63.7% 是**前视造成的**，见第三章。
> 它们的作用是把候选池从几千只缩到几十只，不是告诉你买哪只。**所有胜率都基于 H=20 持有期，不适用于短线。**

---

## 一、八个指标

| 指标 | 抓什么 | 胜率(H=20) | 收益 | 每天信号 | 展示 |
|---|---|---|---|---|---|
| **★★★ 突破预警** | 创60日新高 × 吸筹强 | — | — | 约 80 只 | 主图绿▲ |
| **★★★ SAR预警** | SAR翻多 × 吸筹强 | — | — | 约 20 只 | 主图紫▲ |
| **★★ 拉升预警** | 动力线 × 吸筹强 | — | — | 约 2 只 | 主图青▲ |
| ★ v4 | v3强 × 吸筹强 | 58.3% | +5.57% | 1.2 只 | 主图金▲ |
| 低点组合 v2 | 阶段底部 + 过滤 | 53.0% | +2.96% | 稀疏 | 副图青柱 |
| **买卖很准 周线版** | 周线买线>0 的状态 | — | — | 状态量 | 副图蓝柱 |
| 买卖很准 v3 | 超卖反转 + 过滤 | 50.9% | +2.10% | ~50 只 | 主图红▲ |
| 主力吸筹 | 筹码换手痕迹 | 43.5% ⚠️ | +0.42% | ~200 只 | 副图琥珀柱 |

⚠️ **拉升预警用的是另一把尺子**：目标是【事件】（10 个交易日内触及 +10%），
不是 H=20 收益，所以不填这张表的胜率/收益列。见第二章。

全市场基准胜率 49.0%（H=20）。K线标记：青▲=拉升预警，金▲=v4，红▲=v3（图下沿）；
橙▼=龙虎榜上榜日（图上沿，纯参考不是信号）。

**v5 已于 2026-09-06 下架**（依赖龙虎榜，见第二章）。龙虎榜退回参考位：
只做龙虎榜页面和 K 线上的上榜标记。

---

## 二、每个指标的来历

### 买卖很准 周线版 — 状态而非买点（2026-09-07）

**v3 第一次拿到正贡献，条件是换到周线、并且换一种取法。**

| | 日线版（现行） | 周线版 |
|---|---|---|
| 取法 | 边沿（买线 >0→0） | **状态（买线 >0 的每一周）** |
| 超同日全市场 | −1.00pp | **+3.39pp** |
| 按天/周 t | −3.64 | **+5.56** |
| 九格最小 | −1.04pp | **+0.30pp**（全正） |
| 负年 | 7 | **2**（且都 <2pp） |

（周线数字为可交易口径：下周一开盘买入、持有 8 周）

**三个关键发现**：

1. **取"状态"而不是"起始"**（持有8周）：
   ```
   买线>0 每周   +3.45pp  t=5.05  九格 +0.38  ✅
   状态起始周    +2.51pp  t=2.56  九格 -0.01
   连续第2周     +2.67pp  t=1.32  九格 -0.79
   ```
   价值在"处于超卖状态"这个持续条件本身，不在"刚进入"那一刻。所以它是
   **状态指标**（像均线多头排列），不是事件信号 —— 前端画副图色带而非三角。

2. **持有期单峰在 8 周**（2周 +1.68 / 4周 +3.20 / **8周 +3.45** / 12周 +2.55 /
   16周 +1.86 / 26周 +1.03）。单峰形状比孤立高点可信。

3. **两个负年可解释**：2020(−1.7pp) 与 2023(−1.3pp) 都是极端抱团行情
   （核心资产 / AI 独走），资金集中在少数龙头，而它抓的是超卖反弹的普通股。
   最强的 2021(+11.3) 与 2026(+8.4) 都是轮动行情。失效模式有结构性解释，
   而不是散落无规律 —— 后者才是过拟合的特征。

⚠️ **口径**：信号在周五收盘成立，买入价按下周一开盘算。实测两种口径几乎无差
（+3.45pp → +3.39pp，跳空均值 −0.006%），因为它是状态量、没有消息抢跑；
但**这个检查每个新指标都要单独做** —— 龙虎榜那种盘后信号跳空 +1.31%，
命中率要掉 4.31pp。

> ## ❌ 更正（2026-09-07 复核）：突破预警 2.62 与 SAR预警 1.74 都复现不出
>
> 原脚本是在容器里临时写的，随镜像重建丢失，无从核对当初错在哪。
> 用三种独立方法复核，结论一致 —— 两个指标在**持有期收益**上都跑输全市场：
>
> | 20日持有 | 平均收益 | 胜率 |
> |---|---|---|
> | 全市场基准 | **+1.46%** | 49.2% |
> | 突破预警 | **−0.42%** | 39.6% |
> | SAR预警 | +0.79% | 44.3% |
>
> 加择时也救不回来（基准 +1.51% / SAR +1.27% / 突破 +0.53%）——
> 择时把整个市场都抬高了，信号相对基准仍是负的。
>
> 组合回测：突破 0.17、SAR 0.17。虚拟盘实跑三个月：
> `突破预警-10w` **−6.27%**、`SAR预警-10w` **−11.03%**。
>
> **推测原因**：当初拿「10日内触及+10%」的**事件命中率**当成了**持有期收益**。
> 这两个是"冲一下就回落"的形态 —— 事件命中率高，固定持有反而亏。
> 同类错误本项目在形态模型 v4 上又犯了一次（1.70 全来自买不到的一字涨停）。
>
> **下面这一节的分析（性格差异、九宫格、SAR用在v3上无效）仍然成立，
> 失效的只有那两个组合比值。**

### ★★★ SAR预警 — SAR翻多 × 主力吸筹（2026-09-07）

与突破预警是同一族（都是"趋势确认 × 吸筹"），**性格不同**：

| | 组合比值 | 年化 | 回撤 | 胜率 | 九格最小 |
|---|---|---|---|---|---|
| 突破预警 | ~~2.62~~ ❌ | ~~+39.96%~~ | ~~15.24%~~ | 约 35% | +2.68pp |
| SAR预警 | ~~1.74~~ ❌ | ~~+34.65%~~ | ~~19.96%~~ | **52.5%** | **+3.93pp** |

突破版比值更高但**靠少数大赢家**（胜率 35% 意味着连亏五六笔是常态）；
SAR 版胜率高 17 个点，连亏的串短得多，心理上好拿住。
**九格最小 +3.93pp 是所有组合里最高的** —— 各波动/市值格子都均匀，不挑票。

⚠️ 同样离不开择时：裸跑比值只有 **0.29**（年化 +11.57% / 回撤 40.58%）。

**SAR 用在买卖很准 v3 上是无效的**（这是最初的动机，结果相反）：

```
v3强 单用              超基准 +1.49pp   负年 2   按天t  0.02
v3强 × 近5日SAR翻多     超基准 +0.73pp   负年 4   按天t -0.18   ← 反而更差
```

又一次印证：**主力吸筹是干活的那个，搭档只负责"确认趋势转向"**。
v3 本身太弱，给它加什么都救不回来。

### ★★★ 突破预警 — 创60日新高 × 主力吸筹（2026-09-06）

**第一个组合层面满足「年化 > 回撤」的东西。** 但它成立的前提很硬：**必须配大盘择时**。

与「拉升预警」是一对反向的：

| | 信号日空头排列占比 | 信号级命中率 | 组合(裸跑) | 组合(带择时) |
|---|---|---|---|---|
| 拉升预警（抄底） | **89.1%** | 32.6% | — | 0.71 |
| 突破预警（追势） | **0.0%** | 32.0% | **0.17** | **1.21~1.91** |

（全市场同期空头排列占比 22.6%，作对照）

**信号级两者几乎一样，组合级差了十倍。** 差别全在**相关性**：
突破型信号高度同步，全在同一波行情里触发，裸跑时一起崩（回撤 59.7%）；
但也正因为同步，**一个大盘开关就能整批挡住** —— 择时后回撤降到 21.5%，
年化反而从 10.0% 升到 41.0%。抄底型信号分散在不同时间，择时挡不住，只能压到 0.71。

**择时基准越偏小盘越好，单调**（30仓/止损6%/止盈15%/持10日）：

```
中证1000 > MA20   年化 +39.96%  回撤 15.24%  比值 2.62   ← ❌ 复现不出, 见下方更正
中证1000 > MA10   年化 +39.34%  回撤 15.68%  比值 2.51
中证500  > MA20   年化 +42.69%  回撤 18.90%  比值 2.26
沪深300  > MA20   年化 +29.19%  回撤 15.21%  比值 1.92
等权指数 > MA10   年化 +40.95%  回撤 21.46%  比值 1.91
上证指数 > MA20   年化 +27.84%  回撤 23.80%  比值 1.17
无择时（对照）     年化  +9.99%  回撤 59.69%  比值 0.17
```

**基准必须和标的匹配。** 突破预警选的是小盘股，用沪深300/上证做择时等于拿
蓝筹的脸色判断小盘股死活 —— 中证1000 才是它们的水位线。这条比"用哪根均线"
重要得多（换基准 1.91→2.62，换均线只在 ±0.1 内动）。

**逐年（30仓/MA20口径）八年 0 负年**，包括其他所有方案都亏钱的 2024（+10.1%）：

```
2019 +41.4%  2020 +36.3%  2021 +79.5%  2022 +6.9%
2023 +15.9%  2024 +10.1%  2025 +76.4%  2026 +1.0%
剔除 2025 后仍有 +376.8% —— 不像拉升预警那样靠一年撑着
```

⚠️ **需要泼的冷水**：胜率只有 35.2%，靠少数大赢家，心理上比 58% 胜率难拿住；
54 组参数里只有 7 组过 1.0（13%），参数不是随便选都行；2026 年化 +1.0%，
最近的表现是弱的。

### ★★ 拉升预警 — 动力线 × 主力吸筹（2026-09-06）

**这是全项目第一个逐年 / 按天 t / 九宫格全部通过的指标。**

关键不在于找到了新信号，而在于**换对了尺子**。同样这批东西，在 H=20 收益上
全军覆没，换成事件型标签（10 个交易日内收盘价触及 +10%，基准 17.45%）之后：

| 组合 | 频率 | 命中率 | 超基准 | 逐年 | 按天t | 九格最小 |
|---|---|---|---|---|---|---|
| 动力线 单用 | 18.0/天 | 18.7% | −0.27pp | ❌6年负 | −0.70 | ❌ |
| **吸筹强 单用** | 187.9/天 | 25.7% | +7.93pp | ✅ | **67.35** | ✅ +1.74 |
| 获利盘背离最高档 | 292.1/天 | 20.2% | +2.72pp | ✅ | 25.52 | ✅ +0.11 |
| 背离 × 吸筹 | 70.2/天 | 25.2% | +7.50pp | ✅ | 45.28 | ✅ |
| **动力线 × 吸筹强** | 0.7/天 | **32.6%** | +9.06pp | ✅ | 4.79 | ✅ +1.42 |

（1/3 抽样，全市场频率约乘 3）

**三个反直觉的地方**：

1. **动力线单用几乎无效**（18.7%，基准 17.45%），但叠上去把吸筹从 25.7% 收紧到
   32.6%。它的价值不是当基底，是当最后一道过滤 —— 这跟主力吸筹当初"单独负面
   但共振最强"是同一个道理。
2. **获利盘背离加不了分**（背离×吸筹 25.2% < 吸筹单用 25.7%）。因为主力吸筹
   模型本身就是用筹码特征训的，背离的信息已经在里面，再叠一遍是重复。
3. **主力吸筹一直被低估**。之前标"胜率 43.5% ⚠️ 单独用是负面的"，那是在 H=20
   收益上量的。换成它本来就该干的活，它是全项目最稳的一个（t=67，九格全正）。

**上线前的两项验证**：
- 截断重算 309 次，零不一致（LLV/HHV/EMA 全是向后看的窗口）
- T+1 开盘口径 **33.60%**，比收盘口径 32.59% 还高 —— 次日平均低开 0.09%，
  不像龙虎榜那样有买不到的部分

### 买卖很准 v3 — 超卖反转 + 噪声过滤

**原信号**：`compute_lines` 的买线从 >0 回落到 0（连续超卖状态刚结束）。

**先测 baseline，结论是它本身没有 alpha**：32 万个买点 vs 同期随机对照，
短周期（3–5 天）**输给随机**（−0.006pp），20–40 天才微弱跑赢（+0.2~0.37pp），
胜率 48.6~50.7%，中位数多为负。**均值为正几乎全来自市场 beta。**

这解释了 v2 那次 MCPT p=1.0：不是过滤规则不行，**是被过滤的东西本身就是噪声**。

**v3 的做法**：标签用【同日 × 同波动五分层】内的超额，断掉"挑高波动"的作弊路径。
（v2 失败正因标签只按同日中性化，模型学成了波动率筛子：九宫格分层内 6/9 为负。）

**结果**：胜率 48.9→50.9%，中位 −0.074%→+0.200%，波动层内三档超出全为正。
按天 t=1.39，**仍不显著**。

**持有期 H=20**（2026-09-05 修正，原为 10）。实测扫描：

```
H=5   +1.6pp  t=1.86
H=10  +2.0pp  t=1.35   ← 原设置，是拍脑袋定的
H=20  +2.4pp  t=4.07   ← 现设置
H=40  +1.4pp  t=7.35
```

### 主力吸筹 — 筹码换手痕迹

**目标**：未来 10 日内出现【单日涨幅 >7% 且 量 >20 日均量 2 倍】。价量齐升才算拉升。

**先证伪了两个流行说法**（纯价量维度）：

- 「缩量横盘蓄势」**是错的**：横盘天数最多的 10%，拉升概率仅 **2.87%**（基础 8.30%）
- 「波动压缩后突破」**也反向**：波动最压缩一档 7.73%，波动最大一档 10.90%
- 真正预示拉升的是"**已经在动**" —— 纯价量做不到"提前"，只能"跟随早期"

**突破口是筹码分布**（tushare `cyq_perf`，2018-01 起，950 万行）。有效的**只有获利盘族**：

| 特征 | Q10/Q1 |
|---|---|
| 获利盘变化20 | 2.24 |
| **获利盘背离** = 获利盘变化 − 价格涨幅 | **2.16** |
| ★量能萎缩（价量最强，对照） | 1.70 |
| 筹码集中度 | 1.10 ❌ |
| 低位筹码占比 | 1.01 ❌ |

**「筹码集中度」完全无效**，这推翻了我原本的假设。吸筹不一定让筹码变集中，
但一定会改变**获利盘结构**：价格没动而获利盘上升 = 有人在低位持续接走浮筹。

**增量检验**：价格位置×量能 双重控制的 3×3 九宫格，九格 Q10/Q1 全为正、最小 1.63，
控制前后几乎不衰减（2.16→2.09）→ 筹码携带的是价格和成交量里**没有**的信息。

**样本外**：Q10/Q1 = 4.81，按天 t = 77.4，逐年八年 1.70~2.02 倍无失效。

⚠️ **它单独用是负面的**（H=20 胜率 43.5%、中位 −2.06%）——因为它预测"会不会拉升"，
而拉升前的票往往还在跌。**但它是 v4 最大的增益来源。**

### 低点组合 v2 — 阶段底部 + 过滤

原指标有两类买入信号，实测差异很大：

```
阶段底部(动力线上穿0.2)          H=20 超出随机 +0.557pp  胜率 51.8%  ← 采用
DIBU(13/21/34/55四周期KDJ全<20)  H=5  【输给随机】-0.074pp        ← 弃用
```

**条件更苛刻的 DIBU 反而更差** —— 深度超卖多出现在下跌趋势中段，接的是下落的刀。

**结果**：Top10% 胜率 53.8→57.7%，按天 **t=3.88**（第一个超过 3 的），
波动层内三档 t 全部 >2.5（2.55/2.80/3.74）。

**2026-09-06 补测 —— 原指标其实有 6 个信号，当初只测了 2 个**：
买入侧还有「阶段关注」(动力线上穿 0.5)，卖出侧有 TOBU / 清仓 / 短线卖出。
补测「阶段关注」并顺带扫了阈值（1670 只抽样，H=20，vs 同日全市场）：

```
上穿 0.05     551 个   胜率 57.5%   超出 +4.31pp
上穿 0.10   4,185 个   胜率 59.4%   超出 +1.59pp
上穿 0.15  13,362 个   胜率 56.7%   超出 +1.03pp
上穿 0.20  26,080 个   胜率 54.6%   超出 +0.79pp   ← 现用(阶段底部)
上穿 0.30  52,082 个   胜率 52.2%   超出 +0.38pp
上穿 0.50  82,521 个   胜率 50.6%   超出 +0.16pp   ← 阶段关注, 基本没用
```

**干净的单调剂量反应**——阈值越低越强，这种形状通常说明是真效应而非噪声。
漏掉的「阶段关注」本身很弱，但它带出的阈值曲线说明 **0.10 可能比现用的 0.20 更好**
（胜率 59.4% vs 54.6%，信号量仍有约 1600 个/年）。
⚠️ 这只是原始信号对比，**还没过波动×市值九宫格**，不能直接换。

**跨指标发现**：`筹码宽度` 在本指标里重要性第一（0.130），但在主力吸筹里完全无效
（Q10/Q1=1.10）。**筹码数据源通用，但具体哪个特征有效必须按目标重新筛。**

### 买卖很准 v4 — 共振

**v3 强档（rank≥0.8）与 主力吸筹强档（rank≥0.95）在 ±3 交易日内同时触发。**

不要求同一天：两个指标描述的是同一段行情的不同阶段，触发时点天然错开，
严格同日会漏掉绝大多数真共振。

**三关验证全过**：

```
关1 逐年：2019-2026 八年全部 >54%（最低 2026 年 54.4%）
关2 随机对照：62.5% vs 同日随机 53.2%±0.42，z=22.1
关3 波动×市值九宫格：九格胜率超出全为正，均值 +15.5pp，最小 +11.1pp
```

### 买卖很准 v3.5 — 参数重扫（2026-09-05 已下架）

移植 TDX 时照抄的参数 `MA5/LLV10/连续5`，从没针对 A 股验证过。
36 组网格扫描（MA∈3/5/8 × LLV∈5/10/15/20 × 连续∈3/5/10）后，**它只排 9/33**。

```
全市场 (H=20, vs 同期随机对照)
原参数 MA5/LLV10/5   28.1万信号  胜率 50.9%  超出 +0.190pp
最早   MA3/LLV15/3   35.5万信号  胜率 50.4%  超出 +0.242pp
最准   MA8/LLV20/10   9.5万信号  胜率 56.2%  超出 +0.602pp  ← v3.5 采用
```

**原参数两头不靠**：既不够早，也不够准。

⚠️ **v3.5 是"放大器"**：好年份 +1.0pp、坏年份 −1.0pp（原参数只有 ±0.5pp），
2020/2023/2025 三年跑输随机 —— **普涨行情里严格筛选反而错过**。这是"严格"本身的
固有属性，调参消除不掉。

→ 推论：**库里其他 TDX 移植指标的参数，可能都值得重扫一遍。**

**下架原因（2026-09-05）**：实际用下来没有比 v3 更好。它统计上确实更准（56.2% vs 50.9%，
超出随机 +0.602pp vs +0.190pp），但代价是信号少三分之二、普涨行情跑输、作为共振材料
也比 v3 差。**另外它压根没接进每日增量**（`update_indicators.py` 只跑 v3/吸筹/低点），
数据停在建表那天，再放下去只会变成一个悄悄过期的指标。

表 `maimai35_signal` 和 `build_maimai35.py` 保留，随时可以接回来。

### v5 — 三重共振（2026-09-06 已下架）

`v4` 再叠一层「近 5 日龙虎榜机构净买入 > 0」。

龙虎榜（`top_list`，13 万条）**覆盖率仅 1.2%**（只有异动才上榜），单独用没意义，
但作为第三层过滤：

```
v4  (v3强 × 吸筹强)          n=2,157  胜率 58.3%  收益 +5.57%  1.2个/天
v5  (+ 龙虎榜机构净买入)      n=   58  胜率 62.1%  收益 +6.86%  7.5个/年
(以上为修正后的因果口径; 修正前含前视的数字见第三章)
```

**关键验证 —— 同等涨幅下对比**（排除"异动本身"，因为上榜的票当天本来就在动）：

| 当日涨幅 | 上榜拉升率 | 未上榜 | 差额 |
|---|---|---|---|
| **−2~2%** | 12.21% | 7.26% | **+4.95pp** |
| >9% | 33.21% | 27.18% | +6.03pp |
| 5~9% | 13.57% | 14.37% | −0.80pp |

**「−2~2%」那一档最有价值**：价格几乎没动却上了龙虎榜 = 大资金在换手但价格没反应。
**与「获利盘背离」同一逻辑**，只是这里是实名席位的硬记录，不是从价格反推的。

机构净买入分五档：Q1净卖 14.93% → Q4 **28.63%** → Q5净买 25.20%
—— **不是越多越好**，极端净买入反而回落（可能是游资对倒或已拉过高）。

**下架原因（2026-09-06）**：龙虎榜盘后才公布，这些票次日平均高开 **1.31%**，
「10日涨10%」命中率从 41.53%（收盘基准）掉到 **37.22%**（次日开盘，可交易基准）——
好看的那部分买不到。而 v3/吸筹/低点这类盘中就能算的信号跳空仅 ±0.03%，不受影响。

叠加上前面说的因果口径修正（信号从 609 掉到 58 个），v5 已经既不够频繁、
又有一层买不到的收益。**龙虎榜退回参考位**：只做龙虎榜页面的每日榜单、
和 K 线上的上榜标记，供人工判断，不进任何训练指标。

### 一个反直觉发现：更准的基底 ≠ 更好的共振材料

```
v3   单独 50.9% → 与吸筹共振 58.3%  (1.2个/天)
v3.5 单独 56.2% → 与吸筹共振 57.1%  (0.4个/天)
```

v3.5 单独更强，共振后反而更弱。**共振需要的是足够的信号量 + 独立的误报模式，
而不是单个指标的精度** —— 信号本来就少，再取交集就所剩无几，统计上也更不稳。

---

## 三、这轮最重要的方法论结论

### 0. 从 50.9% 到 62.1% 的完整路径

```
v3   胜率 50.9%   ← 起点
 ├─ 堆 14 个新特征(筹码+市场状态)  → +0.0pp   ❌ 完全没用
 ├─ 持有期 H=10 → H=20            → t 1.35→6.48  ✅
 ├─ 原指标参数重扫 (v3.5)          → +5.3pp   ✅
 ├─ × 主力吸筹共振 (v4)            → +7.4pp   ✅✅
 └─ × 龙虎榜机构净买入 (v5)        → +11.2pp  ✅✅ (但一年只有 7.5 次)
```

(上面是因果口径。含前视时这两步分别是 +12.8pp 和 +25.3pp —— 差额就是偷看未来的部分。)

**最直觉的那条路（堆特征）贡献是 0。** 真正有效的三件事：
**调对持有期、扫对参数、跟独立信号共振。**

### 3. 共振的时间窗只能向后看 —— 一次前视事故

v4/v5 的共振原本写成 `pp.trade_date between mm.trade_date - 5 and mm.trade_date + 5`、
`lb.trade_date between mm.trade_date - 7 and mm.trade_date + 7`。

**这是前视。** T 日的信号要求知道未来 5~7 天会不会出现吸筹信号、会不会上龙虎榜,
而龙虎榜是事后才公布的。当时想的是"两个指标描述同一段行情的不同阶段, 触发时点
天然错开", 这个想法本身没错 —— 错在**错开的方向只能是过去**。

改成只向后看之后:

| 口径 | 信号数 | 胜率 | 均收益 | 超同日全市场 |
|---|---|---|---|---|
| v4 前视 | 5,882 | 64.6% | +7.98% | +5.34pp |
| **v4 因果** | 2,157 | **58.3%** | +5.57% | **+1.93pp** |
| v5 前视 | 609 | 78.0% | +19.30% | +16.73pp |
| **v5 因果** | **58** | **62.1%** | +6.86% | **+2.49pp** |

**v5 有九成的信号是靠未来才成立的。** 7.7 年只剩 58 个, 样本量已经小到没法下结论。

教训: 之前那套验证规矩(随机对照、九宫格、逐年、截断重算)全都跑过了, **但它们
检查的是"信号和收益的关系", 没有一条检查"信号本身是什么时候能知道的"**。
所以第 6 条的截断重算要扩展 —— 不只重算指标值, 还要重算**共振/join 的结果**。

### 1'. 信号级关卡全过 ≠ 组合级可用

这是本轮最重要的一条。那套关卡（逐年 / 按天 t / 九宫格）量的是
「**这个信号预测事件比基准强多少**」，**它完全不管信号之间的相关性**，
而相关性才是回撤的来源。

```
拉升预警  信号级 32.6%（各关全过）  组合级 0.71，回撤 11%
突破预警  信号级 32.0%（各关全过）  组合级 0.17，回撤 60%  ← 裸跑
突破预警  同上 + 大盘择时                  1.91，回撤 21%
```

三者信号级几乎一样，组合级差十倍。**以后新指标必须两级都验：
信号级看有没有预测力，组合级看能不能拿得住。**

### 1. 堆特征救不了坏基底

给 v3 加了 8 个筹码特征 + 6 个市场状态特征，H=10 下还是 **+2.0pp，和原来一模一样**。
因为买卖很准的原信号 vs 随机 ≈ 0，**从噪声里能挖的东西有上限**。

真正有效的是另外两件事：**调持有期**（t 从 1.35→4.07）和**共振**。

### 2. 单独没用的信号，可能是最好的过滤器

主力吸筹单独用胜率只有 43.5%（低于基准 49%），但叠加到 v3 上把它从 50.9% 抬到 62.5%。
**因为两者误报模式不同，共振能互相抵消噪声。**

→ 以后训出新指标，**第一件该做的是测它和现有指标的共振**，而不是单独看它多强。

---

## 四、测过但不用的

拉了数据、跑了验证、结论是不用。记在这里是为了不再重做一遍。

### 机构席位 top_inst —— 数据不够, 无法评估

龙虎榜的席位明细, 想把 v5 的「净买入」细化成「是不是机构专用席位」。

```
覆盖 2018-01 ~ 2019-08(只拉了个开头, 35.7 万行)
落到 v4 信号上: n=13
```

13 个样本什么都测不出来。而且它**没有近期数据, 实盘跟不了** —— 与北向资金停更
同一类问题。要用得重拉七年, 而现在的 `net_amount`(全量)在 v5 里已经工作得很好。

### 股东户数 holdernum —— 数据完整, 但信号是假的

「户数下降 = 筹码集中 = 有人吸筹」, 15.8 万条季度数据, 覆盖 2016 至今。
⚠️ 必须用 **ann_date(公告日)** 而不是 end_date 匹配, 否则是前视。

```
v4 基底                       n=5,882  胜率 64.6%
  + 户数下降(原始)             n=1,316  胜率 65.5%   看似 +0.9pp
  + 截面排名 降最多 20%         n=  614  胜率 65.1%
  + 截面排名 升最多 20%(对照)   n=  607  胜率 66.2%   ← 对照组反而最高
```

**对照组比实验组还好**, 那 +0.9pp 是噪声。而且 59.1% 的季度都在下降(注册制
扩容下的普遍趋势), 原始「下降」本身没有区分度。

另有个结构性问题: **季度披露、滞后一到三个月**, 而 v4/v5 是日频信号。拿两个月前
的快照过滤今天的信号, 时效对不上。

---

## 五、命令与表

```bash
# 全量重建（首次 / 重训，各几分钟）
python -m app.commands.build_maimai    # → maimai_signal (含买卖两向)
python -m app.commands.build_pump      # → pump_signal
python -m app.commands.build_didian    # → didian_signal
python -m app.commands.build_maimai35  # → maimai35_signal

# 每日增量（已接入 scheduler，15:30 收盘后自动跑）
python -m app.commands.update_indicators --days 10
# 该命令依次做: 拉筹码 → 拉龙虎榜 → 重算 v3/吸筹/低点
# ⚠️ 龙虎榜必须拉, 否则 v5 一周后就再也出不了新信号(静默失效)
```

| 表 | 内容 | 量级 |
|---|---|---|
| `maimai_signal` | 买卖很准，含 `side`(buy/sell) | 63.5 万 |
| `pump_signal` | 主力吸筹，只存中/强档 | 164.9 万 |
| `didian_signal` | 低点组合，只存中/强档 | 4.1 万 |
| `maimai35_signal` | 买卖很准 v3.5（参数重扫） | 6.9 万 |
| `top_list` | 龙虎榜每日明细(已接每日增量) | 13.0 万 |

**v4 / v5 无独立表** —— 查询时用 SQL join 实时算（v4 join 两表，v5 join 三表）。

**接口**：`/api/indicators/{maimai|pump|didian|combo|maimai35|v5}/{ts_code}`、
`/api/indicators/trained/screen?indicator=...&grade=...&days=...`
（indicator ∈ v5 / combo / maimai_buy / maimai35 / didian / pump）

---

## 六、验证规矩（每个指标都走完这套）

1. **先测原信号 baseline vs 同期随机对照** —— 不先做这步，等于不知道过滤器在过滤什么
2. **标签按【同日 × 同波动层】中性化** —— 否则模型学成波动率筛子
3. **walk-forward 逐年重训** —— 每个测试点都是真样本外
4. **按天聚合算 t** —— 用信号数当样本量会让 t 虚高 4 倍以上（同一天的信号高度相关）
5. **波动×市值九宫格层内验证** —— 排除"只是挑高波动小市值"
6. **截断重算查前视** —— 把数据砍到 T 日重算，值必须与全量一致。
   ⚠️ 多表 join / 共振类信号必须连 join 结果一起重算 —— 单个指标值没问题，
   不代表组合信号没问题（v4/v5 的前视就是这么漏掉的）。
7. **每个 join 的时间窗都要问"这个数据 T 日收盘时公布了吗"** —— 龙虎榜是盘后
   公布的，窗口用 `T-7 ~ T` 可以，写成 `T-7 ~ T+7` 就是偷看未来。
8. **基准价必须是你真正能买到的价** —— 与第 7 条是两回事。盘后才知道的信号，
   时序上没偷看，但拿 `close[T]` 当买入价仍然错，你最早只能 T+1 开盘买。
   实测「10日涨10%」：龙虎榜的票次日平均高开 **1.31%**，命中率从 41.53% 掉到
   **37.22%**（高估 4.31pp）；而 v3/吸筹/低点这类盘中就能算出来的信号跳空仅
   ±0.03%，几乎无差别。**判断标准：信号是"盘中能算"还是"盘后才公布"。**
9. **跨周期指标要丢掉"尚未走完的那一根"** —— 周线/月线指标在周中重算时，
   当周只有一部分日线（周三跑就只有三天），算出的值与完整周不是一回事，
   到周五还可能翻转。这**不是穿越**（用的数据更少），但与回测对象不一致，
   会让人照着一个没定型的值下单。判据：自然周的周日 ≤ 今天，才算走完。
   ⚠️ 另外 `to_period("W").end_time` 给的是**周日**，永远不是交易日 ——
   落库要存"当周最后一个交易日"，否则前端按日期匹配 K 线一根都对不上且不报错。

## 七、踩过的坑

- **重写已有实现**：为向量化重写 `compute_lines`，`min_periods` 处理不同 → 648 天里
  59 天买线值不一致，漏掉 42% 信号。**凡涉及已有实现一律复用，不重写。**
- **时区**：后端 `calendar.timegm` 生成的 timestamp 是 UTC 基准，前端用
  `setHours(0,0,0,0)` 归一化会整体偏移一天 → 标记早一根 K。**统一用 UTC 日期字符串对齐。**
- **数据覆盖率**：收盘后 K 线渐进入库，最新交易日常只有三分之一成分股到位
  （实测 13/34）。板块均值因此严重失真 → 加了 80% 覆盖率阈值。
- **`/app/fable` 非挂载目录**：容器重建即丢失。研究数据一律放 `/app/data/research/`。
- **构建失败静默沿用旧镜像**：`docker compose up --build` 构建失败仍返回 0。
  **部署后必须验证打包产物含新代码。**
- **三个页面用不同组件**：`ChartArea`(行情/选股) / `LiveView`(实时) / `SectorView`(板块)。
  给其中一个加功能，另外两个**不会自动有**。一天之内因此漏了五次
  （板块条漏两个页面、v4 切换漏两个页面、副图参数、CSS 路径、时区）。
  **凡是改指标菜单或图表叠加，必须三处都过一遍。**
- **`git checkout` 不是撤销键**: 未提交的改动用它回退, 直接没了, 不可恢复
  (2026-09-05 清掉了 MainChart.tsx 一整天的训练指标接线, 只能重写)。
  想撤销刚才的编辑就改回去, 不要动 git。
- **改完要在浏览器里等数据真到位再看**: 标记类改动截图太早会看到"没画出来",
  容易把渲染问题误判成数据问题(v3.5 就这么误诊过一次)。
- **多周期联动切回来指标消失**（三个独立成因，一次修完）：
  联动视图是 `linkedMode ? <LinkedView/> : <MainChart/>` —— 切过去时 MainChart
  被**卸载**，切回来是**全新的图表实例**。三处都因此失效：
  1. 标准指标（MA/BOLL/MACD/SAR）挂在 ChartArea 里，依赖是 `[]` 只跑一次；
     而 ChartArea 自己没卸载 → 加 `linkedMode` 进依赖
  2. 训练副图的 effect 声明在图表 init 之前，挂载瞬间 `chartRef.current` 还是
     null，`if (!chart) return` 之后再也不试 → 加轮询重试（主图标记没这问题，
     因为 `paintTrainedMarkers` 自带重试）
  3. TDX 的 `paneIds` 没随图表销毁清空 → `apply(name, false)` 走
     `overrideIndicator`，而新图表上根本没这个指标，什么也不做 →
     **必须独立成一个 effect** 清空：塞进原来那个 effect 不行，因为它要先轮询
     图表实例，而切【到】联动时主图已卸载、轮询全空就 return，清空逻辑走不到
- **指标叠加是个反复出现的坑，根治办法是"对账"而不是"再创建一次"**：
  klinecharts 主图用 `isStack=true`，重复 `createIndicator` 会往同一面板叠一层，
  而且**不报错**。这个项目已经栽过三次：TDX 切周期叠了 4 层「买卖很准」；
  修联动丢指标时给 effect 加了 `activeIndicators` 依赖，结果 toggle 和 effect
  两条路径一起创建，叠出 5 层 MA。
  **正确写法是让状态同步这件事幂等**：先看图上现在有什么，多的删（含重复的）、
  缺的补；toggle 只改 React 状态，不自己动图表。这样无论重跑多少次、
  图表是新是旧，结果都一样，也不必为"联动切回来是全新图表"写特殊分支。
- **if-else 链没有 default 保护**：切换逻辑写成
  `n==="pump" ? A : n==="didian" ? B : C`，新增第四个选项时会掉进兜底分支 C，
  静默切换了错误的东西且不报错。**行情页用数组 toggle
  (`prev.includes(n) ? 移除 : 添加`)，天然没有这个问题** —— 应该统一成这种写法。

### 坑 · 同一份清单抄了两遍(2026-09-07)

实时页的「训练」菜单只有 4 个指标, 行情页已经有 9 个。原因是清单在
`Toolbar.tsx`(行情页) 和 `IndicatorMenus.tsx`(实时/板块页) 各存了一份,
新增指标只改了前者。同类问题这个项目犯过五次:板块条漏两页、v4 切换漏两页、
v3.5 完全没接数据、SAR/突破/拉升/周线版只接了行情页。

顺带暴露的两个问题:
- `liftalert` 路由从上线起就是 500 —— 函数里漏写局部 `from sqlalchemy import text`。
  只有行情页能触达它, 而那个菜单项没人点过, 所以没人发现。
- 两个菜单的 v4 描述不一致, `IndicatorMenus` 里还是修正穿越【之前】的 62.5%。

**规矩**: 清单和取数都只留一份 —— `hooks/useTrainedSignals.ts`。
新增训练指标只改那一个文件, 三个页面自动跟上。
`text` 之类的公共 import 一律放模块顶层, 不要每个函数各写一次
(写了十次, 第十一次就会漏)。

## 形态模型（shape）— 无 regime 记忆的选股模型（2026-09-07）

用户要求：训练一个识别 K 线形态的模型，**不许对牛熊有记忆**
（"譬如牛市它就买，熊市它什么都不干"），可用 5 年数据训练。

代码：`backend/scripts/shape/{build_features,train,backtest}.py`

### 去记忆是怎么做到的（三层，全部是构造上杜绝，不是训完再查）

1. **特征无身份无时间**：29 个特征全是比值 —— 区间位置、影线占比、量比、
   以 ATR 为单位的距离。没有绝对价格、日期、代码、行业、指数、市值。
2. **每日横截面转分位**：做完之后"全市场普涨"和"全市场普跌"的特征分布
   完全一样，分位只说"这只票今天排第几"。
3. **标签取超额**：未来 H 日收益 − 当日全市场等权均值。

**审计结论（通过）**：模型每日均分 vs 当日大盘涨跌 相关 **−0.0295**，
vs 次日 −0.0313，每日均分标准差 0.009。它确实没在押方向。

### 训练/测试
训练 2016-2020（随机抽 300 万行），测试 2021-2026 全部样本外（755 万行）。

### 结果：选股有效，但做不成策略

样本外分层单调：Top1% **+2.56pp** / Top5% +0.92 / Top20% +0.57 / Bot20% **−1.04**。

组合回测（20 仓位、含成本滑点、T+1）：

| 方案 | 年化 | 回撤 | 比值 |
|---|---|---|---|
| 纯选股 | +8.17% | 66.14% | **0.12** |
| 加止损 5%/8%/12% | 全负 | 77~82% | 负 |
| 对冲中证1000 | −0.16% | 63.17% | −0.00 |

**⚠️ 三个必须记住的结论**：

1. **模型学到的是动量**：特征重要性 pos10/streak/pos5/pos20/pos60 合计 74.8%。
   动量平均超额为正，但会集体崩塌。
2. **止损让它更差**：胜率从 41% 掉到 21% —— 高波动动量股被反复扫出局。
   "加个止损总没坏处"是错的。
3. **alpha 集中在两年**：逐年 Top5% 超额 2021 +2.57 / 2026 +2.08，
   中间 2022~2025 四年是 +0.96/−0.06/+0.30/+0.43，约等于零。
   总数好看是被两年拉起来的。

### 「年化 > 回撤」与「不许择时」在纯多头下是冲突的

测试期中证1000 **自身最大回撤 −46.7%**，同期总涨幅只有 +10.4%。
只做多的组合躲不开这个回撤下限，要满足年化>回撤就得年化>46%。
唯一能躲开的办法是熊市空仓 —— 而那正是用户禁止的行为。

对冲试过了：因为组合 beta 远大于 1（小盘动量股），减 1 倍指数收益中和不掉，
且剥掉 beta 后仍连续四年负 —— 拖累它的是动量因子本身，不是市场 beta。

### 形态模型 v2 — 路径型奖励 + 量能（2026-09-07）

按用户要求改的两件事：奖励机制改成"买入并保持盈利"，加入量能机制。

**奖励**：对每个 (票,t)，看 t+1..t+H 每天的累计超额 cum_s
```
A = mean(cum_s)          净值曲线整体在水上多高
B = -min(0, min cum_s)   最深水下多少
奖励 = A - 1.0·B
```
先跌30%再涨回原点：v1 得 0（终点相同），v2 重罚。

**量能**：新增 11 个无量纲特征 —— 量分位(120日)、放量上涨占比、缩量下跌占比、
量价相关、OBV归一化斜率、主动买盘代理(5/20日)、量堆、地量、突破放量、量能收敛。

#### 结果：奖励确实改变了模型，但方向不是想要的

| | v1（终点收益） | v2（路径奖励+量能） |
|---|---|---|
| 最大单特征重要性 | pos10 **0.207** | pos60 **0.080**（分散多了）|
| 量能类合计 | 12.4% | **40.6%** |
| 去记忆审计 | −0.0295 通过 | −0.0326 通过 |
| Top1% 超额 | +2.56pp | +1.15pp |
| Top5% 超额 | +0.92pp | **+0.12pp** |
| Bot20% 超额 | −1.04pp | **−1.56pp** |
| 组合比值 | 0.12 | **−0.17** |

**⚠️ 三条结论**：

1. **奖励机制起作用了**：动量集中度大幅下降（0.207→0.080），量能占比 12%→41%。
   模型确实换了一套逻辑，不是没学到。
2. **但多头端变弱了**：Top5% 从 +0.92 掉到 +0.12。惩罚"挖坑"的同时也把
   "先蹲后起"的票一起罚掉了 —— 而 A 股的强势股恰恰常常先洗一波。
3. **空头端反而更强**：Bot20% 从 −1.04 到 −1.56，胜率 38.6%。
   **这个模型识别"不该买"比识别"该买"强得多。**

#### 下一步该往哪走（尚未验证）
Bot20% 的强度提示：把它当【排除过滤器】叠在现有策略上，而不是当选股信号。
但逐年一致性还没验（本轮没保存全量分数），不能当结论。

### 形态模型 v4 — 三重障碍标签（2026-09-07）❌ 已证伪，1.70 是假的

用户指出 v2 的病根：**"只罚亏不奖赚，模型就不买了"** —— 完全正确。
但补救方向不是直接奖励 MFE。三步实验：

| 奖励 | Top5% | 逐年负年 | 组合比值 |
|---|---|---|---|
| v1 终点收益 | +0.92pp | 2（且全靠2021/2026） | 0.12 |
| v2 A − MAE（只罚亏） | +0.12pp | 2 | −0.17 |
| v2.5 A − MAE + **MFE×2** | **−0.47pp** | 4 | 更差 |
| **v4 三重障碍** | +0.62pp | **0** | **1.70** |

**⚠️ 为什么加 MFE 反而更差**：MFE 是【吃不到】的利润。固定持有到期按收盘卖，
窗口内那个最高点早还回去了；重奖 MFE = 挑"冲上去再砸下来"的彩票票。

**结论：奖励必须和出场规则对齐，只奖励可兑现的利润。**
（同一个错误此前已犯过一次：SAR/突破用事件命中率验证、却按固定持有回测，
2.62 变 0.17。）

#### 三重障碍
```
上障碍 +15% 先碰 → 实得 +15%    下障碍 −8% 先碰 → 实得 −8%
20 日都没碰      → 按收盘实得
标签 = 该规则下【真正落袋】的超额；回测逐字执行同一套规则
```

#### 结果（训练 2016-2020，测试 2021-2026 全样本外）
```
+15%/−8%, 20仓位:  年化 39.18%  回撤 22.99%  比值 1.70
逐年  2021:+102%  2022:+13%  2023:+19%  2024:+35%  2025:+55%  2026:+12%  ← 零负年
去记忆审计 −0.036 通过；量能类特征占重要性 39%
```

**稳健性**：仓位 10/20/30/40 → 1.71 / 1.70 / 1.23 / 1.16，全部 >1。
障碍扫描单峰：0.08/0.05 → −0.08，0.12/0.06 → 0.77，**0.15/0.08 → 1.70**，
0.20/0.10 → 0.87。去掉最强的 2021 年，其余五年仍全正。

#### ❌ 证伪：1.70 全部来自买不到的一字涨停

补上涨跌停成交约束后（同一模型、同一信号，只改成交假设）：

| 入场口径 | 笔数 | 年化 | 回撤 | 比值 |
|---|---|---|---|---|
| 原始（不检查涨跌停） | 4037 | +39.18% | 22.99% | **1.70** |
| 跳过"开盘即涨停" | 3151 | −4.70% | 48.93% | **−0.10** |
| 只跳过一字板（开盘涨停也买） | 3596 | −26.29% | 86.62% | **−0.30** |

**约 886 笔（22%）一字涨停买入贡献了全部收益，而那些票根本买不到。**

第三行更致命：如果真按涨停价买那些开盘涨停、盘中会打开的票，结果是 −0.30 ——
买在当天最高点然后往下走。那批交易不是"赚不到"，是**实际操作会亏**。

**⚠️ 这条要刻在墙上：模型越准，越会挑中次日一字涨停的票，回测收益就越假。
任何"预测短期大涨"的模型都会撞上这个 —— 成交约束必须在第一次回测就加，
不能等到最后验证。本轮差一步就把它当成结论交出去了。**

三重障碍这个标签设计本身仍然有效（六年零负年、去记忆审计通过），
失效的是"能买到"这个假设。

### 形态模型 v5 + 当过滤器用（2026-09-07）

**v5 = 把"能不能买到"写进标签**：入场价改成次日开盘真实成交价，次日一字涨停
的样本直接从训练集剔除（不是打低分，是让模型见都见不到）。

用户的判断"你这个训练是在追涨"是对的：标签定成"20日内涨15%"，模型必然去学
追涨，而 A 股能涨 15% 的票次日多半买不到。

| 版本 | 成交约束 | 组合比值 |
|---|---|---|
| v4 三重障碍 | 无 | ~~1.70~~（假） |
| v4 三重障碍 | 有 | −0.10 |
| **v5 可成交标签** | 有 | **0.24** |

修正有效（−0.10 → 0.24），但差 4 倍。

**信号层很干净**：Top5% +0.45pp，六年零负年，逐年最小 +0.08pp，
去记忆审计 +0.006（几乎正好 0）。**问题是量级**：
每笔 20 日 +0.45pp × 年周转 12 次 ≈ 毛 5~6pp/年，成本吃掉 3~4pp，
净 alpha 约 2pp/年 —— 扛不住中证1000 同期 46.7% 的回撤。

#### 当过滤器：有效但边际

Bot20%（每日打分最差 20%）当排除名单，叠在周线版上（同一引擎、同一口径）：

| | 年化 | 回撤 | 比值 | 胜率 |
|---|---|---|---|---|
| 周线版 原版 | 10.95% | 58.94% | 0.19 | 45.8% |
| **+ Bot20% 排除** | 12.80% | 57.12% | **0.22** | **49.7%** |

剔掉 19.6% 的信号。**胜率 +3.9pp 是最扎实的一项**；最差的几年明显改善
（2022 −33→−24，2023 −12→−5，2026 −17→+2），但 2021 +13→−11、
2024 +13→+1 反而变差 —— 不是干净的胜利。

⚠️ 这里的 0.19 与虚拟盘账户的 0.56 不矛盾：引擎不同
（成交额下限 2000万 vs 500万、本轮加了涨跌停不可成交约束）。
可比的只有同一行内的 原版 vs 过滤版。

#### 已接入生产（2026-09-07）

形态模型现在只以【过滤器】身份存在，不作选股信号：

| 件 | 说明 |
|---|---|
| `shape_score` 表 | 每日全市场打分 + 当日横截面分位 |
| `app/commands/build_shape_scores.py` | 每日打分。⚠️ 必须算全市场——分位是横截面概念，单只算没意义 |
| `sync_mmweek_signals.py --filtered` | 写 `mmweek_f`：周线版信号 join `shape_score` 剔掉 `rank_pct < 0.20` |
| 虚拟盘 `周线版-过滤-10w` (id 111) | 与 `周线版-10w` (id 108) **并行对照**，不替换 |

⚠️ 每日任务里顺序不能颠倒：先打分，`mmweek_f` 才 join 得上。
⚠️ 模型文件在 `/app/data/research/shape/`（挂载卷）。放 `/app` 别处重建镜像就没了。

**生产链一致性已验**：与研究阶段的 Bot20% 名单在重叠日重合 92.7% / 96.1%
（差额来自研究集末尾被标签窗口截断，覆盖股票少）。

前 3 个月对照（多数仓位未平，仅供观察不作结论）：
原版 +13.02% / 已平仓均收益 −8.68% / 胜率 0.0%；
过滤版 +11.88% / 已平仓均收益 **−2.37%** / 胜率 **37.5%**。

### 虚拟盘引擎：涨停买不进的判定补全（2026-09-07）

原来只查一处：
```python
if closes[0] > closes[1] * 1.099:   # 信号日涨停
    continue
```
但**买入发生在次日开盘**，没查买入当日是不是一字涨停 —— 逻辑上的缺口。

形态模型上吃过大亏：22% 的买入是次日一字涨停，而那批**不可成交**的单子
贡献了全部收益，组合比值 1.70 补上约束后变成 −0.10。

现在两处都查：信号日涨停（尾盘买模式的约束）+ 买入当日一字涨停
（次日开盘买模式的真正约束）。

**实测影响极小**（这两个策略几乎不碰涨停票）：

| | 买入笔数 | 买在涨停日 | 一字板 | 改前总收益 | 改后 |
|---|---|---|---|---|---|
| 拉升预警回测 | 549 | 13 (2.4%) | 0 | +165.32% | +165.73% |
| 周线版回测 | 390 | 2 (0.5%) | 2 | +269.10% | +273.09% |

⚠️ **改后反而略高** —— 挡掉的是买在一字涨停上的单子，那种单子实际是亏的
（买在当天最高点）。与形态模型的结论一致，只是幅度小到可以忽略。

价值不在这两个策略，在于**以后新上的策略不会再重蹈形态模型的覆辙**。

#### 形态模型的日线出口（2026-09-07 补）

此前模型只接在**周线版**上当过滤器（`mmweek_f`），而模型本身是
**日线特征、每日打分、20 个交易日持有** —— 日线这边一个出口都没有。

新增 `shape` 策略 = 每日打分 Top1%，账户 `形态模型-日线-10w`(112)。

⚠️ **出场规则逐字照抄训练标签**（三重障碍 +15% / −8% / 20 交易日）：
```
stop_pct 0.08 · tier1_pct 0.15 tier1_frac 1.0（到 +15% 一次清仓）
max_hold_days 18（×1.6≈29 自然日≈20 交易日）
```
规则一改，模型优化的东西和账户执行的东西就不是一回事了。

⚠️ **回测判据没过**（带成交约束 0.24）。建这个账户不是因为验过了，
而是**向前验** —— 回测已经骗过我一次（v4 的 1.70 全来自买不到的一字涨停），
真实前瞻数据是唯一干净的检验。

前 3 个月（+5.21%，115 笔已平仓）执行得干净：

| 出场 | 笔数 | 均收益 | 胜率 |
|---|---|---|---|
| 止损 −8% | 59 | −8.06% | 0% |
| 止盈 +15% | 35 | +15.17% | 100% |
| 到期 | 21 | +3.61% | 61.9% |
| 合计 | 115 | **+1.14%** | 41.7% |

三条路径的均收益正好落在 −8 / +15 / 居中，说明规则没走样。
买入 135 笔中仅 4 笔碰上涨停日。
